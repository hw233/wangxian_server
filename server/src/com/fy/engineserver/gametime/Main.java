package com.fy.engineserver.gametime;

public class Main {

	static long times = Integer.MAX_VALUE;

	public static void main(String[] args) {
		// AbstractGameTime gameTime = GameTimeFactory.createCurrentTime(SourceType.SELF);
		// gameTime.start();
		//
		// long count = 0;
		// while (true) {
		// gameTime.currentTimeMillis();
		// if (++count == times) {
		// gameTime.cancel();
		// break;
		// }
		// }
		// System.out.println(gameTime.getAverageVisitTimesInfo(TimeDistance.SECOND));
		// System.out.println(gameTime.toString());

	}
}
